//animation text

const typed = new Typed('.multiple-text', {
    strings: ['Physical Fitness', 'Weight Gain', 'Fat Loss', 'Weight Lifting', 'Running'],
    typeSpeed: 50,
    backspeed: 60,
    backdelay: 1000,
    loop: true,
  });


  // page animation 

AOS.init({
    offset: 300,
    duration: 1400
  });
  

